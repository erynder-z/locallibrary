<h1><%=title%></h1>
<ul>
    <% if(book_list){ %> <% for(const book of book_list) {%>
    <li>
        <a href="<%=book.url%>"><%=book.title%></a>
    </li>
    <% } %> <% } else{ %>
    <li>There are no books</li>

    <% } %>
</ul>
