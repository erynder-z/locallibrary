<h1>Title: <%=book.title %></h1>
<p>
    <strong>Author:</strong>
    <a href="<%=book.author.url%> "><%=book.author.name%></a>
</p>
<p><strong>Summary: </strong><%=book.summary%></p>
<p><strong>ISBN: </strong><%=book.isbn%></p>
<p>
    <strong>Genre: </strong>
    <% if(book){ %> <% for(const [i, val] of book.genre.entries()) {%> <% if (i
    < book.genre.length-1) { %>
    <a href="<%=val.url%>"><%=val.name%>,</a>
    <% } else { %>
    <a href="<%=val.url%>"><%=val.name%></a>
    <% } %> <% } %>
</p>

<div style="margin-left: 20px; margin-top: 20px">
    <h4>Copies:</h4>

    <% if(book){ %> <% for(const val of book_instances) {%>
    <hr />

    <% if(val.status == "Available") { %>
    <p class="text-success"><%=val.status %></p>
    <% } else if (val.status == "Mainternance") { %>
    <p class="text-danger"><%=val.status %></p>
    <% } else { %>
    <p class="text-warning"><%=val.status %></p>
    <% } %>
    <p><strong> Imprint: </strong><%=val.imprint %></p>
    <% if (val.status != "Available") { %>
    <p><strong>Due back: </strong><%=val.due_back %></p>
    <% } %>
    <p>
        <strong>Id: </strong>
        <a href="<%=val.url  %> "><%=val.id %> </a>
    </p>
    <% } %> <% } %> <% } else{ %>
    <p>There are no copies of this book in the library.</p>

    <% } %>
</div>
