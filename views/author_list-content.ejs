<h1><%=title%></h1>
<ul>
    <% if(author_list.length > 0){ %> <% for(const author of author_list) {%>
    <li>
        <a href="<%=author.url%>"><%=author.name%></a> <%=author.lifespan %>
    </li>
    <% } %> <% } else{ %>
    <p>There are no authors</p>

    <% } %>
</ul>
